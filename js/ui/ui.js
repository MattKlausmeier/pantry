import { install, authorSpan } from './install.js'
import { fg1 } from './fg.js'
import { init } from './pagemanager.js'

export const ui = {
    Install: install,
    AuthorSpan: authorSpan,
    Fg1: fg1
}

export const pm = {
    Init: init
}
